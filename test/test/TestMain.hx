package;

import imported.Imported;

class TestMain {
    public function new() {
        addObject(ExceptionTest);
        addObject(Prompt);
        addObject(assert.TestSuite);
        addObject(Imported);
        addObject(threading.A);
        addObject(threading.B);
    }
}
