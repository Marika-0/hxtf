# Configured or Haxe 3.4.7

-cp src

-main hxtf.Hxtf

-dce full
-D analyzer

-cpp build/cpp

-cmd rm -rf build/ready
-cmd cp -rf resources/framework build/ready
-cmd cp -f  build/cpp/Hxtf build/ready/hxtf
